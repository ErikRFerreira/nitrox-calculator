/** Default O₂ percentage — represents air/unanalysed tank. User should set after analysing. */
export const DEFAULT_O2 = 21;

/** Default Helium percentage. */
export const DEFAULT_HE = 0;

/** Default Helium percentage for Trimix mode — pre-selects the first Trimix preset. */
export const DEFAULT_TRIMIX_HE = 35;

/** Default partial pressure of oxygen limit. */
export const DEFAULT_PPO2 = 1.4;
